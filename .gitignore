# Build outputs and CMake caches (regenerated by build.bat / cmake --build)
/build/
/build2/
/out/
.vs/

# Vendored third-party SDKs and libraries (fetched via vcpkg or downloaded upstream,
# not hand-edited project source - see README/CMakeLists for how to re-fetch them).
/third_party/
/libs/

# Misc local editor/OS junk
*.user
*.suo
Thumbs.db
